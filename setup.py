'''
setpy.py is used to turn a folder into an installable package.
 with this python file become a proper library.
 setup.py tells Python:

The name of the library (myproject).

Where the code lives (myproject/ folder).

Dependencies needed (numpy, pandas).

Pip copies your package into site-packages (Python’s library folder).

Same place where numpy, pandas, etc. live.

Now you can use it anywhere like a real library
'''